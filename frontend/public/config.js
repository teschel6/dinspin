// Local/dev default. In the production image this file is regenerated at
// container startup from the API_URL env var (see docker/config.js.template
// and frontend/Dockerfile) so VITE_API_URL no longer needs to be baked in.
window.__APP_CONFIG__ = {}
